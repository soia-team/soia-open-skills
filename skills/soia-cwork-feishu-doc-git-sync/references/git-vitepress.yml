version: 1

git:
  repository_is_user_supplied: true
  commit_after_validation: explicit_confirmation
  push_after_commit: explicit_confirmation
  never_force_push: true

vitepress:
  generated_sidebar: 90_同步元数据/sidebar.json
  route_prefix: /feishu-knowledge/
  build_command: npm run docs:build
  dev_command: npm run docs:dev

obsidian:
  vault_dir: docs/feishu-knowledge
  open_in_obsidian: user_action
  generated_dir: 10_飞书镜像
  manual_dir: 20_本地补录
