interface:
  display_name: "Dev Agent CLI Dispatch"
  short_description: "Dispatch coding/review/analysis sub-tasks to external coding CLIs (codex, gemini, kimi, opencode, qwen, claude) with controlled workdir, anti-injection prompts, model tiering, and Anti-Fake-Fix verification."
  default_prompt: "Use soia-dev-agent-cli-dispatch: dispatch a coding/review/analysis sub-task to an external coding CLI (codex/gemini/kimi/opencode/qwen/claude) with controlled workdir, background monitoring, and Anti-Fake-Fix verification."
