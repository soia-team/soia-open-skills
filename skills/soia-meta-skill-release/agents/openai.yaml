interface:
  display_name: "Meta Skill Release"
  short_description: "完成正式发布收尾；默认只发布，按客户选择转交定向安装。"
  default_prompt: "Use $soia-meta-skill-release for remote-only release by default; ask before delegating any local install selection."
