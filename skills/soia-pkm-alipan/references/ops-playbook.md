# 阿里云盘整理战役实战手册（ops-playbook）

> 来源：2026-07-01~07 一周内完成两盘→单盘、9.21TB 全盘序号化整理的实战沉淀。补充 SKILL.md 未覆盖的坑与方法论，遇到同类场景直接照此执行，不必重新试错。

## 一、安装与登录

### 1.1 安装
```bash
brew install aliyunpan      # macOS 官方 formula
brew upgrade aliyunpan      # 已装则升级到最新
aliyunpan --version         # 确认版本
```

### 1.2 登录（两步授权 + 扫码）
`aliyunpan login` 走扫码登录，分两步：
1. CLI 打印一个二维码（或授权链接），需要**用户本人**用阿里云盘 App 扫码授权
2. 授权后 CLI 自动完成登录，写入登录态到 `~/.config/aliyunpan/`

登录**必须由用户本人操作**——代理不能替用户扫码，只能引导用户完成这一步。

### 1.3 登录态约 3 天过期
**症状**：任意命令（`ls`/`quota`/`who`）报错或返回异常，常见提示词含「未登录账号」类字样。
**处理**：不要反复重试或换参数——这是过期而非命令写错，直接告知用户「登录态过期了，需要重新扫码」，引导跑 `aliyunpan login`。实战中一周整理战役期间发生过 1 次过期（傍晚时段），当场重登即恢复，不影响已完成的操作结果。

### 1.4 非交互环境取登录链接的技巧
纯脚本/后台代理环境里没有真 TTY，`aliyunpan login` 可能拿不到可扫的二维码输出。两个可行技巧：
- **伪终端（pty）**：用 `script`/`expect`/Python `pty.spawn` 包一层伪终端运行 `aliyunpan login`，让 CLI 以为自己在交互终端里，从而正常吐出二维码链接文本
- **交互进程读 stdout**：把 `aliyunpan login` 作为长驻子进程启动，持续读取其 stdout 流（而不是等它退出再读），二维码链接一出现就截取，转给用户点击或扫码

两种技巧本质都是"让 CLI 相信自己在被人类使用"，核心是不要用一次性 `subprocess.run` 等待退出码——那样会拿不到中间打印的二维码。

### 1.5 `--driveId` 显式传参铁律
**绝不使用 `aliyunpan drive <id>` 切换全局当前盘**——该命令会修改 CLI 的**全局状态**（写配置文件）。如果同时有多个代理/多个脚本会话在跑，一个会话切盘会污染其他所有并发会话的当前盘上下文，造成"明明在操作备份盘却影响到资源库"的诡异错误。

正确做法：**每条命令都显式带 `--driveId`**：
```bash
aliyunpan ls --driveId 278255980 "/10_孩子学习库/"
aliyunpan mv --driveId 278255980 "/源" "/目标目录/"
```
这是本次整理战役全程多代理并发执行的安全前提，务必写进任何批量脚本的第一行原则。

## 二、批量操作的实战坑

### 2.1 批量 rename 的 cd 依赖坑
CLI 的部分命令（尤其相对路径操作）**依赖 CLI 自身维护的"当前目录"上下文**，不是每次都能无脑传绝对路径生效。脚本里如果用 `cd` 语义切换当前目录、又在循环体内出错中断，会导致 CLI 的当前目录状态和脚本预期不一致，后续命令全部作用在错误目录上。

**恢复方法**：
1. 出现异常后**先不要继续批量跑**，单条 `ls` 当前预期目录，确认 CLI 实际所在位置
2. 每条命令一律用**完整绝对路径**，不依赖 `cd` 累积状态（哪怕啰嗦一点）
3. 批量脚本改成"无状态"风格：每次调用都从根路径重新拼接完整路径，而不是"cd 进去再操作相对路径"

### 2.2 `ll` 输出含 FILE ID —— 直达链接的来源
`aliyunpan ll`（详细列表，区别于 `ls`）输出里带**文件/目录的 file_id**，可直接拼成网页版直达链接：
```
https://www.alipan.com/drive/folder/<file_id>     # 目录
https://www.alipan.com/drive/file/<file_id>       # 文件
```
本次整理战役里 8 张叶级地图、00_馆藏总览、全部馆藏卡的 `drive_link` 字段，都是靠批量 `ll` 抓 file_id 拼接生成——不要手工去网页版一个个复制链接，效率天差地别。

### 2.3 移动/改名不改 file_id；跨盘移动会换 file_id
- **同盘内 `mv`/`rename`**：file_id 不变，之前生成的直达链接依然有效——意味着"先扫描生成地图，之后再批量整理挪目录"是安全的，链接不会失效
- **跨盘移动**（如资源库→备份盘）：CLI 不支持跨盘 `mv`，只能用户在 App 里手动拖动；App 内跨盘迁移**会重新生成 file_id**，迁移前生成的所有直达链接全部失效，必须迁移完成后**重新扫描重建地图**。本次单盘化（资源库整体迁入备份盘）就属于这种情况，影视/书籍文化两张地图因此重扫重建。

### 2.4 删除进回收站 30 天；回收站清空才释放配额
`aliyunpan rm` 不是物理删除，进回收站，30 天内用户可在 App 里恢复。**配额不会因为 rm 而立即释放**——本次整理约清理 140GB，但配额数字要等用户在 App 里手动清空回收站后才真正下降（`quota` 从 8.06TB→7.92TB 是清空回收站之后才体现）。告知用户这一点，避免"删了但配额没变"的困惑。

## 三、全盘 JSONL 爬虫方法论

需要"摸清楚整个盘/整个大目录到底有什么"（叶级全量索引，为整理方案打底）时，单条 `ls` 逐层人工点不现实，用脚本做全盘 DFS 爬虫落 JSONL。

### 3.1 基本方法论
- **DFS 遍历**：Python 脚本对目录树做深度优先遍历，每进一层就 `ls`/`ll` 该层，子目录压栈继续下探，文件直接落记录
- **每行一个 JSON 对象**（JSONL 格式），字段至少包含：路径、名称、类型（文件/目录）、大小、file_id、层级
- **nohup 后台跑**：全盘扫描耗时可能几十分钟到几小时（本次技术成长一个区就 3.34TB/38164 文件），必须 `nohup python scan.py > scan.log 2>&1 &` 丢后台，不要占着前台阻塞会话
- **断点续扫**：脚本设计成幂等/可恢复——已扫过的路径记录在 JSONL 里，重启后先读已有结果、跳过已扫路径，只继续扫未完成部分，避免登录过期等中断后要从头重扫
- **progress 心跳**：脚本定期（如每扫 N 个目录）打印一条进度日志（已扫层数/目录数/耗时），方便后台监控进度、判断是否卡死
- **errors 落盘**：扫描失败的路径（报错/超时/异常）单独落一个 errors 文件，不要让单点失败中断整个扫描

### 3.2 两大实战坑

**坑1：登录瞬断导致成批报错**
扫描过程中登录态过期或网络瞬断，会导致连续一大批目录同时报错（不是单条偶发失败）。**处理**：把这类成批报错的路径原样记入 errors 文件，扫描完一轮后**事后补扫**——单独针对 errors 里的路径重新跑一遍，通常此时登录已恢复，能补齐；不要在扫描进行中中断重连（容易进一步搞乱状态）。

**坑2：目录名连续/结尾空格 → "指定目录不存在"**
云盘里历史遗留的目录名可能带**连续空格**或**结尾空格**，直接用这个名字去 `ls` 会报"指定目录不存在"，即便这个目录在 App 里明明存在。**处理**：不要在这类目录上死磕重试——记录下来跳过，在生成的地图/索引里明确标注"⚠️ 目录名疑似含特殊空格，跳过未扫描，需用户在 App 内核实"，留给人工用 App 处理（改名去空格后即可正常访问）。

### 3.3 海量碎片区的聚合剪枝
遇到单个目录下文件数极多（本次「40_AI与工具」一个区 96092 个文件、「60_编程课程35合集」一个区 38164 个文件）的情况，**不要逐文件展开生成索引**——文档体积会失控（本次已实测触发工具读取长度限制，需要分段读取）。

规则：**>200 文件的目录，在生成的地图/模板文档里只记一条"聚合摘要条"**（目录名 + 项数 + 总大小 + 代表性内容举例几个），不逐文件列出；需要看具体某个文件时通过直达链接进网盘该目录人工浏览。这条经验线适用于任何"资料包/合集/训练营"类深层碎片区。

## 四、防代理卡死纪律

多代理并发跑长任务时，两条纪律是本次整理战役实测有效、必须坚持：

1. **长内容一律脚本生成落文件，不要塞进对话轮次**：全盘扫描结果、地图内容、批量操作日志，都应由脚本直接写入目标文件（如 JSONL/Markdown），而不是脚本打印到 stdout 后再靠对话轮次一段段搬运——后者极容易撑爆单轮响应或截断丢失数据。
2. **对话回复限 15 行**：执行代理向上汇报进度/结果时，回复正文控制在 15 行以内（关键结论 + 数字 + 下一步），详细清单/长表格一律落文件，回复里给文件路径。这条纪律用于防止多代理并发场景下主控/复核方被信息量淹没，也降低代理自身因生成超长文本而卡死或超时的风险。
