interface:
  display_name: "ProcessOn 图表助手"
  short_description: "可断点、可审计地递归盘点 ProcessOn，并安全导出图表。"
  default_prompt: "Use $soia-cwork-processon-diagrams to inventory this ProcessOn space in resumable batches, persist and audit the discovered-minus-visited queue, export confirmed flowcharts as VSDX and mind maps as XMind when authorized, and never claim completion before the final audit passes."
