interface:
  display_name: "ProcessOn 图表助手"
  short_description: "递归盘点 ProcessOn 到叶子文件，默认导出 Visio 并校验归档。"
  default_prompt: "Use $soia-cwork-processon-diagrams to recursively inventory this ProcessOn team space through every child folder and leaf file, report the discovered-minus-visited gap, default approved flowchart downloads to VSDX when the live menu supports it, and finalize browser-reported downloads without changing remote content."
