---
name: your-skill-name
description: 一句话说明这个 skill 做什么、何时触发。包含具体触发场景，保持简短，不写个人路径、账号或私有偏好。
version: 0.1.0
created_at: <YYYY-MM-DD HH:mm:ss>
updated_at: <YYYY-MM-DD HH:mm:ss>
created_by: <model-name>
updated_by: <model-name>
---

# your-skill-name

用 1-2 句话说明这个 skill 的定位：它解决什么问题、输入是什么、输出是什么、和其他 skill 的关系是什么。

## 设计约束

- 保持本文件是可独立执行的核心流程；把供应商差异、长清单和背景知识放到 `references/`，并从这里直接链接。
- 让自由度匹配风险：分析可保留判断空间；配置项写成参数；删除、覆盖、发送、发布或修改远端状态必须支持预览/确认边界。
- 先定义验收证据，再执行。复杂流程至少用一个 fixture 或真实前向测试核对实际输出，不把“命令返回 0”当成完成证据。
- 保持正文简洁，目标不超过 500 行；不要在技能目录中新增 README、安装指南、CHANGELOG、QUICK_REFERENCE 或 ARCHITECTURE。
- 日志和最终回复只报告必要的状态、数量、类别和问题；不输出本地绝对路径、逐个本地文件名、用户名、密码、密钥、cookie 或 token。

## 客户可读说明

### 这个技能可以做什么

用客户能听懂的话说明这个 skill 可以帮客户完成什么工作、需要客户提供什么输入、最终会交付什么文件/报告/命令结果/回执。

| 客户想要 | 技能会做 | 客户能看到 |
|---|---|---|
| `<客户场景 1>` | `<执行动作>` | `<文件 / 日志 / 总结 / 状态变化>` |
| `<客户场景 2>` | `<执行动作>` | `<文件 / 日志 / 总结 / 状态变化>` |

删除不用的行。不要把会影响客户判断的行为藏在实现细节里。

### 客户如何使用

1. 客户用自然语言说明目标，并提供必要输入：文件、URL、repo、workspace、proposal、vault 或平台账号状态。
2. Agent 先判断是否命中本技能，再检查依赖、配置、权限和风险动作。
3. 能 dry-run 或预览的动作先给预览；涉及删除、覆盖、发送、发布、写远端状态时先征求客户确认。
4. 执行后验证真实输出，不用“看起来成功”代替证据。
5. 最终回复必须给客户可见总结：做了什么、日志摘要、文件变化、问题和下一步。

### 依赖与安装

区分强依赖、可选增强、方法参考和非依赖。

| 依赖 | 类型 | 安装 / 配置 | 缺失时怎么处理 |
|---|---|---|---|
| `<hard-dependency>` | 强依赖 | `<install command or URL>` | 停止并告诉客户如何补齐 |
| `<optional-provider>` | 可选增强 | `<install command or URL>` | 降级执行，并说明少了什么能力 |
| `<method-reference-skill>` | 方法参考 | 不需要安装，除非客户明确要求使用 | 只复用方法，不调用它 |
| `<non-dependency>` | 非依赖 | 无 | 只用于避免误解 |

配置约定：

```text
~/.config/soia-skills/<repo>/<skill-type>/<skill-name>/config.yml
SOIA_<TYPE>_<SHORT>_CONFIG_FILE=<custom-config-path>
```

- 如果本技能不需要私有配置，可以不创建 `config.yml`。
- API key、cookie、session、provider home、本机路径只放私有 `config.yml`、进程环境或 provider 自己的登录态里。
- 如果依赖第三方 skill，记录上游来源和安装方式，不直接修改第三方 skill 文件。

### 私密信息与中间数据

- 凭据只写入用户自有的私有配置、进程环境或 provider 登录态，不进入 skill、日志或交付物。
- 中间产物按 [DATA_STORAGE_SPEC.md](../../DATA_STORAGE_SPEC.md) 约定落盘，并说明位置、保留期与脱敏要求。

### 日志与完成回执

每个 workflow 必须让客户看到证据。终端输出或最终回复至少包含：

- 开始做什么，使用了哪个配置/ provider，但不打印秘密值。
- 关键进度：数量、当前阶段、输出类别、跳过/失败原因；涉及本地资源时只给泛化位置，不列本地绝对路径或文件名。
- 最终总结：成功 / 跳过 / 失败数量，创建或更新了哪些资源类别。
- 下一步：需要客户确认、补 key、安装依赖或继续运行的具体命令。

推荐最终回复格式：

```markdown
完成：<一句话说明本次完成了什么>。

日志摘要：
- started: <检查到的输入/配置/依赖，不打印秘密值>
- processed: <数量或范围>
- created/updated: <数量或路径>
- skipped/failed: <数量和原因>

文件变化：
- <数量和资源类别；不列本地绝对路径、文件名或账号信息>

验证：
- <运行过的检查、命令或人工核对点>

问题与下一步：
- <缺 key / 缺依赖 / 需要客户确认 / 建议下一条命令；没有则写“无”>
```

## Safety Rules

- Do not hardcode maintainer paths, local checkout paths, vault directory names, account names, secrets, cookies, session strings, or private context.
- Put user-specific behavior in CLI args, environment variables, or the skill-specific user-owned `config.yml`.
- For public skills, document variable names only. For private SOIA skills, follow this repo's `SKILL_SPEC.md` private config/config policy. Never commit real values.
- Read only the user-provided source files or the current task workspace.
- Use placeholders in examples: `<path>`, `<repo-path>`, `<workspace>`, `<YOUR_KEY>`.

## When Triggered

1. Parse the user request into `source`, `target`, `provider`, `output`, and optional `params`.
2. If a provider, path, or credential is missing, check [references/providers.md](references/providers.md) and guide setup without reading private secret files.
3. Execute the smallest reliable workflow.
4. Validate the real output before claiming success.
5. Report what changed, visible log summary, which checks ran, and any remaining gaps.

## Inputs

```yaml
source: <url-or-path>
target: <artifact-or-action>
params:
  provider: auto
  output_dir: auto
  dry_run: false
```

## Resources

- Provider setup and fallback rules: [references/providers.md](references/providers.md)
- Optional config example: [assets/config.example.yml](assets/config.example.yml)
- Optional config resolver: `scripts/resolve_config.py`

Delete unused resource bullets. Do not add README / INSTALL / CHANGELOG inside a skill.

## Agent Metadata

`agents/openai.yaml` is optional UI/catalog metadata only. Keep required capabilities, dependencies, setup, workflow steps, log expectations, and completion summary rules in this `SKILL.md` so Claude Code, Codex, SOIA, and other skills.sh-compatible agents all receive the same instructions.

## Validation

- Static: run `python3 scripts/resolve_config.py --json` if the skill uses config.
- Content: verify output exists and matches the requested target.
- Safety: scan for hardcoded paths and secrets before commit.
